# Automated Newsletter

This project automates the creation and sending of newsletters.

## Structure
- `.github/workflows`: CI/CD workflows
- `src`: Source code
- `templates`: HTML templates

## Usage
1. Install dependencies: `pip install -r requirements.txt`
2. Run scripts in `src/` to fetch news, build, and send.
